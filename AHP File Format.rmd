---
title: "AHP File"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo=FALSE, eval=TRUE, message=FALSE, warning=FALSE)
```

```{r echo=FALSE, eval=TRUE, message=FALSE, warning=FALSE}
packages = c('R6','data.tree','ahpsurvey','ahp')
for (p in packages){ 
  if(!require(p, character.only = T)){
    install.packages(p) 
  }
  library(p,character.only = T) 
}
```

#Reference : https://cran.rstudio.com/web/packages/ahpsurvey/vignettes/my-vignette.html

```{r }
library(R6)
library(data.tree)
library(ahp)
accessAhp <- Load("finalahp.txt")
```

```{r}
```